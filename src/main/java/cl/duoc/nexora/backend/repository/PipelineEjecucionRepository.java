package cl.duoc.nexora.backend.repository;

import cl.duoc.nexora.backend.model.PipelineEjecucion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PipelineEjecucionRepository extends JpaRepository<PipelineEjecucion, Long> {
}
